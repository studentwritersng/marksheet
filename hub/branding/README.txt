# School branding

Drop the school logo here as `logo.png` (also supported: `logo.jpg`, `logo.jpeg`, `logo.webp`, `logo.svg`).

Set `schoolName` in `hub/config.json` to the school name.

Both are shown on the student login page, the invigilator page, and the browser tab title (+ favicon).
