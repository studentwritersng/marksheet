node_modules/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/capacitor.config.json

*.keystore
*.jks
# google-services.json is the FCM client config (no server secret); commit it so
# CI builds always include Firebase config without relying on a repo secret.
!android/app/google-services.json
